{
  "name": "AliveText",
  "description":"Edits text on webpages in realtime time to enhance readability and comprehension.",
  "version": "1.0",
  "manifest_version":2,
  "content_scripts": [
    {
      "matches": ["http://*/*"],   //what URLs to match, we want to match all
      "js": ["script.js"]       // our JS code, inserted at end of document
    }
  ],
  
}